<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= card.title %> - Decision Cards Game</title>
    <style>
        @font-face {
            font-family: 'Nekst';
            src: url('/fonts/NekstRegularDesktop.otf') format('opentype');
            font-weight: 400;
        }

        @font-face {
            font-family: 'Nekst';
            src: url('/fonts/NekstLightDesktop.otf') format('opentype');
            font-weight: 300;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Nekst', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: #FAFAFA;
            min-height: 100vh;
            padding: 40px 20px;
        }

        .container {
            max-width: 700px;
            margin: 0 auto;
            background: white;
            border-radius: 4px;
            padding: 40px;
            box-shadow: 0 2px 12px rgba(0, 0, 0, 0.08);
            border: 1px solid #E8E8E8;
        }

        .breadcrumb {
            color: #666;
            font-size: 0.95em;
            margin-bottom: 20px;
        }

        .breadcrumb a {
            color: #2C5F7F;
            text-decoration: none;
        }

        .breadcrumb a:hover {
            text-decoration: underline;
        }

        h1 {
            color: #1A1A1A;
            font-size: 2em;
            margin-bottom: 30px;
            line-height: 1.4;
            font-weight: 400;
        }

        .card-content {
            background: #f9f9f9;
            border-left: 4px solid #2C5F7F;
            padding: 20px;
            border-radius: 4px;
            margin-bottom: 30px;
            color: #666;
            line-height: 1.6;
            font-size: 1.05em;
        }

        .card-content strong {
            color: #1A1A1A;
        }

        .actions {
            display: flex;
            flex-direction: column;
            gap: 15px;
            margin-bottom: 30px;
        }

        .action-btn {
            display: block;
            padding: 15px 20px;
            text-align: center;
            text-decoration: none;
            border-radius: 4px;
            font-weight: 400;
            font-family: 'Nekst', sans-serif;
            transition: transform 0.2s, box-shadow 0.2s;
            border: none;
            cursor: pointer;
        }

        .select-btn {
            background: #F0F0F0;
            color: #2C5F7F;
        }

        .select-btn:hover {
            background: #E0E0E0;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .select-btn.selected {
            background: #5C9D8F;
            color: white;
        }

        .select-btn.selected:hover {
            background: #457B6B;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(92, 157, 143, 0.2);
        }

        .view-results {
            background: #2C5F7F;
            color: white;
        }

        .view-results:hover {
            background: #1F4560;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }

        .locked {
            background: #ddd;
            color: #999;
            cursor: not-allowed;
        }

        .locked:hover {
            transform: none;
            box-shadow: none;
        }

        .lock-icon {
            margin-right: 8px;
        }

        .navigation {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
            margin-top: 30px;
            padding-top: 30px;
            border-top: 1px solid #E8E8E8;
        }

        .nav-btn {
            padding: 12px;
            background: #f0f0f0;
            color: #2C5F7F;
            text-decoration: none;
            border-radius: 4px;
            text-align: center;
            font-weight: 400;
            font-family: 'Nekst', sans-serif;
            transition: background 0.2s;
        }

        .nav-btn:hover {
            background: #e0e0e0;
        }

        .home-link {
            grid-column: 1 / -1;
            background: #5C9D8F;
            color: white;
        }

        .home-link:hover {
            background: #457B6B;
        }

        .status-badge {
            display: inline-block;
            padding: 6px 12px;
            background: #ffeaa7;
            color: #333;
            border-radius: 20px;
            font-size: 0.85em;
            margin-top: 15px;
        }

        @media (max-width: 768px) {
            body {
                padding: 30px 15px;
            }

            .container {
                max-width: 100%;
                padding: 30px 20px;
                border-radius: 4px;
            }

            .breadcrumb {
                font-size: 0.85em;
                margin-bottom: 15px;
            }

            h1 {
                font-size: 1.6em;
                margin-bottom: 20px;
                line-height: 1.3;
            }

            .card-content {
                padding: 15px;
                margin-bottom: 20px;
                font-size: 0.95em;
            }

            .action-btn {
                padding: 12px 16px;
                font-size: 0.95em;
            }

            .navigation {
                gap: 10px;
                margin-top: 20px;
                padding-top: 20px;
            }

            .nav-btn {
                padding: 10px;
                font-size: 0.9em;
                border-radius: 4px;
            }

            .status-badge {
                padding: 5px 10px;
                font-size: 0.8em;
            }
        }

        @media (max-width: 480px) {
            body {
                padding: 15px 10px;
            }

            .container {
                padding: 20px 15px;
                border-radius: 8px;
                box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
            }

            .breadcrumb {
                font-size: 0.8em;
                margin-bottom: 12px;
                overflow: hidden;
                text-overflow: ellipsis;
                white-space: nowrap;
            }

            h1 {
                font-size: 1.3em;
                margin-bottom: 15px;
                line-height: 1.3;
            }

            .card-content {
                padding: 12px;
                margin-bottom: 15px;
                font-size: 0.9em;
                line-height: 1.5;
                border-radius: 3px;
            }

            .actions {
                gap: 10px;
                margin-bottom: 20px;
            }

            .action-btn {
                padding: 12px 14px;
                font-size: 0.85em;
                border-radius: 4px;
            }

            .lock-icon {
                margin-right: 6px;
            }

            .navigation {
                display: flex;
                flex-direction: column;
                gap: 8px;
                margin-top: 15px;
                padding-top: 15px;
            }

            .nav-btn {
                padding: 10px;
                font-size: 0.85em;
                border-radius: 4px;
                width: 100%;
            }

            .home-link {
                grid-column: auto;
                margin-top: 5px;
            }

            .status-badge {
                padding: 5px 8px;
                font-size: 0.75em;
                margin-top: 10px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="breadcrumb">
            <a href="/">Home</a> <% if (roundNumber) { %> / <a href="/round/<%= roundNumber %>">Round <%= roundNumber %></a> / <a href="/round/<%= roundNumber %>/stakeholder-actions">Stakeholder Actions for Round <%= roundNumber %></a><% } else { %> / <a href="/stakeholder-cards">Stakeholder Actions for Round 1</a><% } %> / Card <%= card.id %>
        </div>

        <h1><%= card.title %></h1>

        <div class="card-content">
            <p><strong>STAKEHOLDER PERSPECTIVE — [INSERT YOUR SCENARIO HERE]</strong></p>
            <p>This is placeholder text. Replace with your stakeholder-focused scenario context, their perspective on the situation, their concerns, and the key decision or challenge they're facing. Consider how this stakeholder views the same situation differently than staff members might.</p>
        </div>

        <div class="actions">
            <button id="selectBtn" class="action-btn select-btn" onclick="toggleSelection()">
                <span id="selectIcon">☐</span> <span id="selectText">Mark as Selected</span>
            </button>
            <% if (roundNumber) { %>
                <a href="/round/<%= roundNumber %>/stakeholder-actions/<%= card.id %>/results" class="action-btn view-results">
                    <span class="lock-icon">🔓</span>View Results
                </a>
            <% } else { %>
                <a href="/stakeholder-cards/<%= card.id %>/results" class="action-btn view-results">
                    <span class="lock-icon">🔓</span>View Results
                </a>
            <% } %>
        </div>

        <script>
            const cardKey = 'card_<%= roundNumber || 1 %>_stakeholder_<%= card.id %>';

            function toggleSelection() {
                const selected = localStorage.getItem(cardKey);
                if (selected) {
                    localStorage.removeItem(cardKey);
                    updateButton(false);
                } else {
                    localStorage.setItem(cardKey, 'true');
                    updateButton(true);
                }
            }

            function updateButton(isSelected) {
                const btn = document.getElementById('selectBtn');
                const icon = document.getElementById('selectIcon');
                const text = document.getElementById('selectText');
                if (isSelected) {
                    btn.classList.add('selected');
                    icon.textContent = '☑';
                    text.textContent = 'Selected';
                } else {
                    btn.classList.remove('selected');
                    icon.textContent = '☐';
                    text.textContent = 'Mark as Selected';
                }
            }

            window.addEventListener('load', () => {
                const isSelected = localStorage.getItem(cardKey);
                updateButton(!!isSelected);
            });
        </script>

        <div class="navigation">
            <% if (prevId) { %>
                <% if (roundNumber) { %>
                    <a href="/round/<%= roundNumber %>/stakeholder-actions/<%= prevId %>" class="nav-btn">← Card <%= prevId %></a>
                <% } else { %>
                    <a href="/stakeholder-cards/<%= prevId %>" class="nav-btn">← Card <%= prevId %></a>
                <% } %>
            <% } else { %>
                <div style="grid-column: 1;"></div>
            <% } %>
            <% if (nextId) { %>
                <% if (roundNumber) { %>
                    <a href="/round/<%= roundNumber %>/stakeholder-actions/<%= nextId %>" class="nav-btn">Card <%= nextId %> →</a>
                <% } else { %>
                    <a href="/stakeholder-cards/<%= nextId %>" class="nav-btn">Card <%= nextId %> →</a>
                <% } %>
            <% } else { %>
                <div style="grid-column: 2;"></div>
            <% } %>
            <% if (roundNumber) { %>
                <a href="/round/<%= roundNumber %>/stakeholder-actions" class="nav-btn">Back to Stakeholder Actions</a>
                <a href="/round/<%= roundNumber %>/team-actions" class="nav-btn">Go to Team Actions</a>
                <a href="/round/<%= roundNumber %>" class="nav-btn home-link">← All Actions</a>
            <% } else { %>
                <a href="/stakeholder-cards" class="nav-btn">Back to Stakeholder Actions</a>
                <a href="/staff-cards" class="nav-btn">Go to Team Actions</a>
                <a href="/" class="nav-btn home-link">← Home</a>
            <% } %>
        </div>
    </div>
</body>
</html>
