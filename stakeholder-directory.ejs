<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stakeholder Cards - Decision Cards Game</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 40px 20px;
        }

        .container {
            max-width: 900px;
            margin: 0 auto;
        }

        .header {
            background: white;
            border-radius: 12px;
            padding: 40px;
            margin-bottom: 40px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .header h1 {
            color: #333;
            margin-bottom: 20px;
            font-size: 2em;
        }

        .nav-btn {
            display: inline-block;
            padding: 12px 30px;
            background: #667eea;
            color: white;
            text-decoration: none;
            border-radius: 6px;
            margin: 0 10px;
            transition: background 0.2s;
        }

        .nav-btn:hover {
            background: #764ba2;
        }

        .home-btn {
            background: #999;
        }

        .home-btn:hover {
            background: #777;
        }

        .cards-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }

        .card {
            background: white;
            border-radius: 12px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s, box-shadow 0.2s;
            cursor: pointer;
        }

        .card:hover {
            transform: translateY(-8px);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.15);
        }

        .card-number {
            display: inline-block;
            width: 50px;
            height: 50px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            font-size: 1.5em;
            margin-bottom: 15px;
        }

        .card-title {
            color: #333;
            font-size: 1.1em;
            font-weight: 600;
            margin-bottom: 15px;
            line-height: 1.4;
        }

        .card-link {
            display: block;
            padding: 10px;
            background: #f5f5f5;
            color: #667eea;
            text-decoration: none;
            border-radius: 4px;
            text-align: center;
            font-weight: 600;
            transition: background 0.2s;
        }

        .card-link:hover {
            background: #667eea;
            color: white;
        }

        .footer {
            background: white;
            border-radius: 12px;
            padding: 20px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        }

        .footer a {
            color: #667eea;
            text-decoration: none;
            margin: 0 10px;
        }

        .footer a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>👥 Decision Cards for Stakeholders</h1>
            <div style="margin-top: 20px;">
                <a href="/" class="nav-btn home-btn">← Home</a>
                <a href="/staff-cards" class="nav-btn">Go to Staff Cards →</a>
            </div>
        </div>

        <div class="cards-grid">
            <% cards.forEach(card => { %>
                <a href="/stakeholder-cards/<%= card.id %>" style="text-decoration: none;">
                    <div class="card">
                        <div class="card-number"><%= card.id %></div>
                        <div class="card-title"><%= card.title %></div>
                        <div class="card-link">View Card →</div>
                    </div>
                </a>
            <% }); %>
        </div>

        <div class="footer">
            <a href="/">← Home</a>
            <a href="/staff-cards">Staff Cards →</a>
        </div>
    </div>
</body>
</html>
